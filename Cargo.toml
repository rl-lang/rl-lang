[package]
name = "rl-lang"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
log = { version = "0.4", features = ["release_max_level_off"] }
env_logger = "0.11"

[[bin]]
name = "rl"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

